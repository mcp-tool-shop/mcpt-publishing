/**
 * Shared constants for fixer modules.
 * Prefixed with `_` so the fixer registry auto-discovery skips this file.
 */

/** Canonical MCP Tool Shop site URL — used in homepage links and README headers. */
export const SITE_URL = "https://mcptoolshop.com";
